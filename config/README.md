# Configuration Directory

This directory contains configuration files for the SR-OS AutoPost Remote Executor Node system.

## Files

- **deployments.json**: Generated after running deployment script, contains deployed contract addresses and ABIs
- Network-specific configuration files can be added here for different environments (mainnet, testnet, etc.)

## Usage

After deploying contracts using `npm run deploy`, the deployment information will be saved here automatically.
